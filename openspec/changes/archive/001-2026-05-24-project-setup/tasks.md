# Tasks: 001-project-setup

- [x] 1. Initialize Rust Workspace
    - [x] 1.1 Create `Cargo.toml` workspace file.
    - [x] 1.2 Create `lmha-core`, `api`, and `scheduler` crates.
- [x] 2. Initialize Database Schema
    - [x] 2.1 Set up `migrations` folder.
    - [x] 2.2 Create migration for `tenants` table.
    - [x] 2.3 Create migration for `devices` table.
    - [x] 2.4 Create migration for `telemetry` table.
- [x] 3. Basic Boilerplate
    - [x] 3.1 Setup `dotenv` and configuration loading in `core`.
    - [x] 3.2 Basic "Hello World" entry points for API and Scheduler.
